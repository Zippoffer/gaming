doctype
html
	head
		title Tickie Tackie Toe
		link(rel='stylesheet', href='/main.css')
		body
			h1 ticatacatoe

			h2 Select a game
				ul
				
				for game in games
					li: a(href=`/game/${game._id}`) Game: #{game._id}
		a(href='/') Home
